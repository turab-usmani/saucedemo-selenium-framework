# Target base URL
BASE_URL=https://www.saucedemo.com/

# Browser options: chrome, edge
BROWSER=chrome

# Execution mode: true for headless, false for headed browser
HEADLESS=true

# Default explicit wait timeout in seconds
DEFAULT_TIMEOUT=10
